NOTE: we have stopped maintaining the x86 version of xv6, and switched
our efforts to the RISC-V version
(https://github.com/mit-pdos/xv6-riscv.git)

xv6 is a re-implementation of Dennis Ritchie's and Ken Thompson's Unix
Version 6 (v6).  xv6 loosely follows the structure and style of v6,
but is implemented for a modern x86-based multiprocessor using ANSI C.

ACKNOWLEDGMENTS

xv6 is inspired by John Lions's Commentary on UNIX 6th Edition (Peer
to Peer Communications; ISBN: 1-57398-013-7; 1st edition (June 14,
2000)). See also https://pdos.csail.mit.edu/6.828/, which
provides pointers to on-line resources for v6.

xv6 borrows code from the following sources:
    JOS (asm.h, elf.h, mmu.h, bootasm.S, ide.c, console.c, and others)
    Plan 9 (entryother.S, mp.h, mp.c, lapic.c)
    FreeBSD (ioapic.c)
    NetBSD (console.c)

The following people have made contributions: Russ Cox (context switching,
locking), Cliff Frey (MP), Xiao Yu (MP), Nickolai Zeldovich, and Austin
Clements.

We are also grateful for the bug reports and patches contributed by Silas
Boyd-Wickizer, Anton Burtsev, Cody Cutler, Mike CAT, Tej Chajed, eyalz800,
Nelson Elhage, Saar Ettinger, Alice Ferrazzi, Nathaniel Filardo, Peter
Froehlich, Yakir Goaron,Shivam Handa, Bryan Henry, Jim Huang, Alexander
Kapshuk, Anders Kaseorg, kehao95, Wolfgang Keller, Eddie Kohler, Austin
Liew, Imbar Marinescu, Yandong Mao, Matan Shabtay, Hitoshi Mitake, Carmi
Merimovich, Mark Morrissey, mtasm, Joel Nider, Greg Price, Ayan Shafqat,
Eldar Sehayek, Yongming Shen, Cam Tenny, tyfkda, Rafael Ubal, Warren
Toomey, Stephen Tu, Pablo Ventura, Xi Wang, Keiichi Watanabe, Nicolas
Wolovick, wxdao, Grant Wu, Jindong Zhang, Icenowy Zheng, and Zou Chang Wei.

The code in the files that constitute xv6 is
Copyright 2006-2018 Frans Kaashoek, Robert Morris, and Russ Cox.

ERROR REPORTS

We don't process error reports (see note on top of this file).

BUILDING AND RUNNING XV6

To build xv6 on an x86 ELF machine (like Linux or FreeBSD), run
"make". On non-x86 or non-ELF machines (like OS X, even on x86), you
will need to install a cross-compiler gcc suite capable of producing
x86 ELF binaries (see https://pdos.csail.mit.edu/6.828/).
Then run "make TOOLPREFIX=i386-jos-elf-". Now install the QEMU PC
simulator and run "make qemu".


一、运行环境与结果
运行环境: Ubuntu (WSL) + QEMU
运行命令: `make clean && make qemu-nox`
运行结果: 系统成功启动，并在内核进入时正确打印可视化信息：[KERNEL] main() started init: starting sh $
二、xv6 启动流程说明
通过本次实验对核心代码的阅读与注释，我总结 xv6 的启动流程如下：
1. 引导阶段 (Bootasm.S): CPU 从 16 位实模式切换到 32 位保护模式，初始化段寄存器。
2. 加载阶段 (Bootmain.c): 引导程序读取磁盘上的 ELF 格式内核镜像，并将各段加载到内存指定位置。
3. 入口阶段 (Entry.S): 开启页表映射，将内核从物理低地址映射到虚拟高地址，并跳转到 `main` 函数。
4. 初始化阶段 (Main.c): 内核完成内存分配、驱动程序、中断向量、多核启动等初始化，最后启动第一个用户进程 `init`。
 三、实验总结
1. Commit 记录: 仓库包含 3 次以上有意义的 Commit，涵盖了环境修复、功能开发及代码注释。
2. 可视化实现: 成功在内核关键节点插入 `cprintf` 打印，直观验证了内核的加载与运行。
3. 心得体会: 
 - 深刻理解了引导程序 512 字节的限制，学习了如何通过硬件端口直接进行打印。
 - 掌握了 GitHub Token 的使用以及远程仓库的管理。
 - 通过对汇编代码的注释，理清了计算机从加电到进入操作系统的核心步骤。


第二次：
第一层：机制观察（全部完成）
    任务 1：系统调用路径跟踪。通过在 `syscall.c` 和 `sys_write` 中添加日志，追踪了 `write` 系统调用的执行路径
    任务 2：调度过程观察。在 `scheduler()` 中添加日志，观察到了进程之间的切换现象
    任务 3：内存分配观察。在 `kalloc.c` 中记录页分配情况，观察了内核物理内存的分配动态
第二层：机制理解（完成任务 1
    系统调用扩展，成功在 xv6 中新增了 `hello()` 系统调用，并编写了用户层测试程序进行验证
    做法：1.在 `user.h` 中添加了 `int hello(void);` 声明，并在 `usys.S` 中添加了 `SYSCALL(hello)` 入口
         2.在 `syscall.h` 中定义了系统调用号 `#define SYS_hello 22`，并在 `syscall.c` 的函数指针数组中注册了 `sys_hello`
         3.在 `sysproc.c` 中实现了 `sys_hello` 函数，使用内核级打印函数 `cprintf` 输出 "hello xv6!"
         4.编写了 `hello.c`，调用 `hello()` 接口并使用 `exit()` 正常退出
         5. 修改 `Makefile`，在 `UPROGS` 列表中加入 `_hello\`，确保新程序被编译进文件系统
         运行截图：/home/kkhoo/xv6-public/hello.jpg

问题：通过配置 GitHub 的个人访问令牌并调整远程仓库 URL，克服了网络环境下的推送难题，发现镜像网站适合用于加速下载，对上传请求的身份验证支持不佳。
心得：本次实践是一次从“理论”跨越到“实操”的深度探索。在上学期分析启动流程的基础上，这周我真正进入了 xv6 的运行内部。
起初，面对复杂的 Git 环境配置和 Linux 命令行，我感到有些棘手。尤其是当 GitHub 推送失败时，深刻体会到了工程实践中“环境配置”往往是第一道难关。在解决这些问题的过程中，我学会了如何耐心排查报错信息，并利用 PAT 等工具解决权限问题，这不仅是技术上的提升，更是心理素质的锻炼。
在实现 `hello` 系统调用的过程中，我最直观地理解了“特权级切换”的概念。以前只在书本上看到用户态通过中断进入内核态，而现在，通过修改 `usys.S` 和 `syscall.c`，我亲手搭建了这条通道。看到屏幕上跳出 `hello xv6!` 的那一刻，操作系统不再是一个黑盒，而是一个由一个个精巧函数和表项勾连起来的生命体。
此外，通过对调度和内存分配的观察，我理解了操作系统是如何在后台默默处理那些繁琐的资源管理工作的。虽然这些日志信息看起来有些杂乱，但它们正是系统高效运行的证据。这次实验让我明白，阅读内核代码固然重要，但动手修改并观察反馈才是掌握底层机制最快的方式。在未来的学习中，我将继续保持这种“动手拆解”的精神。